const getCurrentType = state => state.extraInfo.currentType;
const getCurrentIndex = state => state.extraInfo.currentIndex;
const getCurrentBtnMenu = state => state.extraInfo.currentBtnMenu;
const getCurrentPage = state => state.extraInfo.currentPage;

export { getCurrentType, getCurrentIndex, getCurrentBtnMenu, getCurrentPage };
